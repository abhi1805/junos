---
- name: test playbook
  hosts: all
  connection: netconf
  tasks:
  - name: Push command
    junos_command:
      commands: show version
    register: variable
  - name: Print
    debug:
      var: variable

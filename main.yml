---
- name: test playbook
  hosts: '{{ edge }}'
  connection: netconf
  gather_facts: false
  roles:
  - Juniper.junos
  tasks:
  - name: Test role
    junos_command:
      commands: show version
    register: variable
  - name: Print version
    debug:
      var: variable

---
- name: test playbook
  hosts: all
  connection: netconf
  gather_facts: no
  tasks:
  - name: load configure lines into device
    junos_config:
      lines:
        - set vlans vlan01 description "Test vlan"
